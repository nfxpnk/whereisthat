# Save Performance Task 1 Classification

This table records the save-path callers reviewed before splitting small metadata edits from full catalog staging.

| command/action | file | function | current behavior | target behavior | confidence |
| --- | --- | --- | --- | --- | --- |
| Create disk group | `src/app/wit_gui/src/CatalogWorkflowController.cpp` | `CatalogWorkflowController::CreateDiskGroup` | Calls `Database::CreateWorkingCopy`, applies `CreateDiskGroup` to the staged database, then `CatalogSession::AcceptPending`. | Small metadata edit, but kept on the full staged path for this pass because the UI needs a pending group id before save. | Medium |
| Move disk to group | `src/app/wit_gui/src/CatalogWorkflowController.cpp` | `CatalogWorkflowController::MoveDiskToGroup` | Previously called `Database::CreateWorkingCopy`, applied `MoveDiskToGroup`, then `CatalogSession::AcceptPending`. | Small metadata edit. Record `MoveDiskToGroup` in `CatalogSession` and save it with a short SQLite transaction. If a full staged database already exists, apply to that staged database. | High |
| Move disk group to group | `src/app/wit_gui/src/CatalogWorkflowController.cpp` | `CatalogWorkflowController::MoveDiskGroupToGroup` | Previously called `Database::CreateWorkingCopy`, applied `MoveDiskGroupToGroup`, then `CatalogSession::AcceptPending`. | Small metadata edit. Record `MoveDiskGroupToGroup` in `CatalogSession` and save it with a short SQLite transaction. If a full staged database already exists, apply to that staged database. | High |
| Scan/add/update disk image | `src/app/wit_gui/src/ScanCoordinator.cpp` and `src/app/wit_gui/src/CatalogWorkflowController.cpp` | `ScanCoordinator::Start`, `CatalogWorkflowController::OnScanComplete` | Calls `Database::CreateWorkingCopy`, performs scan changes on the staged database, then `CatalogSession::AcceptPending`. | Scan/full-catalog operation. Keep existing full staging and replacement save path. | High |
| Save pending changes | `src/modules/wit_catalog/src/CatalogSession.cpp` | `CatalogSession::SavePending` | Previously required `pendingDatabase` and always called `Database::SaveCatalogDataFrom`. | Route by pending type: full staged databases still use `SaveCatalogDataFrom`; metadata-only edits use `BEGIN IMMEDIATE`, apply SQL updates, and commit. | High |
| Full staged catalog replacement | `src/modules/wit_database/src/Database.cpp` | `Database::SaveCatalogDataFrom` | Backs up a staged database to a temp catalog, verifies it, prepares single-file catalog state, replaces the active file, and reopens. | Scan/full-catalog and maintenance-style replacement path. Do not use for metadata-only saves. | High |
| Storage smoke staged-save tests | `tests/unit/storage/StorageSmokeTests.cpp` | `StorageSmoke.CatalogDatabaseScannerAndCoordinatorIntegration` | Directly exercises `CreateWorkingCopy` and `SaveCatalogDataFrom` for scanner/full-staging behavior. | Test coverage for scan/full-catalog operation; keep using full staging. | High |
| Storage smoke hierarchy tests | `tests/unit/storage/StorageSmokeTests.cpp` | `StorageSmoke.CatalogDatabaseScannerAndCoordinatorIntegration` | Directly exercises `CreateWorkingCopy` before group/disk hierarchy operations on a database object. | Database-unit coverage of SQL behavior, not an app workflow save path. Leave as direct storage tests. | High |
| Imported catalog move workflow test | `tests/unit/storage/StorageSmokeTests.cpp` | `StorageSmoke.ImportedCatalogGroupAndDiskMovesSaveThroughAppWorkflow` | App workflow for disk/group moves. Previously depended on full staged save. | Regression coverage for metadata-only app workflow; profiler counters must show no working copy, temp backup, integrity check, or `SaveCatalogDataFrom`. | High |

