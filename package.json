{
  "name": "whereisthat",
  "version": "1.0.0",
  "private": true,
  "description": "Tooling for repository formatting and consistency checks.",
  "license": "MIT",
  "scripts": {
    "format": "prettier --write \"*.{md,json,yml,yaml}\"",
    "format:check": "prettier --check \"*.{md,json,yml,yaml}\""
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
